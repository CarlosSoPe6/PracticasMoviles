import 'package:flutter/material.dart';

class CrearNoticia extends StatelessWidget {
  const CrearNoticia({Key key}) : super(key: key);

  // TODO Formulario modelo Noticia, cámara o galeria,
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text("Crear noticia"),
      ),
    );
  }
}
